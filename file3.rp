i64 x = 10 + 20;
add(is_pseudo_prime(5, 1));

fn is_pseudo_prime(): void {
  if (x <= 1) {
    return false;
  }
  if (x == 2) {
    return true;
  }

  for (i64 i = 2; i < x; i = i + 1) {
    if (x / i == 1) {
      return false;
    }
  }

  return true;
}

fn add(i64 x, i64 y): i64 {
  return x + y;
}

print(add(is_pseudo_prime(5, 1)));
i64 x = a && b && c;
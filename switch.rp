i64 x = 8;
f64 y = 4.0;
str text;

switch (x + 5: temp, y) {
  (x < 10 && y < 5.0) -> {
    text = "Less than 5.";
  }
  (temp < 10 && y < 10.0) -> {
    text = "Less than 10.";
    break;
  }
  (x < 15 && y < 15.0) -> {
    text = "Less than 15.";
    break;
  }
}
